/* Single source of truth for the roadmap phase.
   Update PHASE here and the status ribbon + simulator indicator follow. */
export const PHASE = 3;
export const TOTAL_PHASES = 7;

export const PHASE_LABEL = `Pre-launch — Phase ${PHASE} of ${TOTAL_PHASES}`;
export const STATUS_RIBBON = `Status: Pre-launch — Phase ${PHASE} of ${TOTAL_PHASES}. The SHE Score methodology is published; no token has been issued.`;
